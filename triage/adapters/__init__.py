from __future__ import annotations

# triage.adapters — framework adapter modules
# Each submodule (langgraph, crewai, openai_agents, langchain) has a lazy
# framework import and is NOT imported here to avoid hard dependencies.
